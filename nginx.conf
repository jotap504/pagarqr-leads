# -------------------------------------------------
#  Nginx – Reverse proxy for PagarQR Streamlit app
# -------------------------------------------------

server {
    listen 80;
    server_name www.pagar.ar pagar.ar localhost;

    # Proxy to the Streamlit container (port 8501)
    location /search/ {
        proxy_pass http://scraper:8501/search/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_redirect off;
    }

    # Redirect / to /search/
    location = / {
        return 301 /search/;
    }
}

# Comenta este bloque si aún no tienes los certificados configurados en el host
# server {
#     listen 443 ssl;
#     server_name www.pagar.ar pagar.ar;
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     location /search/ {
#         proxy_pass http://scraper:8501/search/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
